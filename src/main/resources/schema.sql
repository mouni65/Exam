CREATE TABLE IF NOT EXISTS BOOK_TABLE(book_id VARCHAR(255) NOT NULL, title VARCHAR(255), author VARCHAR(255), year VARCHAR(255), PRIMARY KEY (book_id));
insert into book_table (book_id, title,author,year) values (1,'book1','author1','2020');
